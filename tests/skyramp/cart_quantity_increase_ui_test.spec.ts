// Generated by Skyramp v1.3.19 on 2026-04-19 03:09:19.497183967 +0000 UTC m=+168.383559149
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output cart_quantity_increase_ui_test.spec.ts \
// 		--output-dir /home/runner/work/microservices-demo-testb/microservices-demo-testb/tests/skyramp \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/microservices-demo-testb/microservices-demo-testb/.skyramp/cart_quantity_increase_trace.zip

// Import of required libraries
import { test } from '@playwright/test';
import { expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:8080");
    await page.goto("http://localhost:8080/product/OLJCESPC7Z");
    await page.locator('#quantity').selectOption("2");
    await page.getByRole("button", { name: "Add To Cart" }).click();
    await expect(page.getByRole("main")).toContainText("2");
    await page.getByRole("button", { name: "+" }).click();
    await expect(page.getByRole("main")).toContainText("3");
    await expect(page.getByRole("main")).toContainText("$59.97");
    await expect(page.getByRole("main")).toContainText("Cart (3)");
});
