// Generated by Skyramp v1.3.19 on 2026-04-19 03:13:49.080543717 +0000 UTC m=+373.248169261
// Command: skyramp generate ui rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output search_currency_ui_test.spec.ts \
// 		--output-dir /home/runner/work/microservices-demo-testb/microservices-demo-testb/tests/skyramp \
// 		--overwrite \
// 		--playwright-trace /home/runner/work/microservices-demo-testb/microservices-demo-testb/.skyramp/search_currency_ui_trace.zip

// Import of required libraries
import { test } from '@playwright/test';
import { expect, newSkyrampPlaywrightPage } from '@skyramp/skyramp';

const pageTimeout = 15000;

test('testUi', async ({ page }) => {
    test.setTimeout(60000);

    const errors: string[] = [];
    page.on('pageerror', (err: Error) => errors.push(err.message));

    // set default timeout for all locator/page actions in this test
    page.setDefaultTimeout(pageTimeout);
    // Wrap playwright page with Skyramp page
    page = newSkyrampPlaywrightPage(page);

    await page.goto("http://localhost:8080");
    await page.getByRole("combobox").selectOption("EUR");
    await page.getByRole("textbox", { name: "Search products..." }).fill("Sunglasses");
    await page.getByRole("button", { name: "Search" }).click();
    await expect(page.getByRole("heading", { name: "Search results for " })).toContainText("Search results for");
    await expect(page.getByText("€17.68")).toContainText("€");
    await expect(page.getByRole("textbox", { name: "Search products..." })).toHaveValue("Sunglasses");
    expect(errors).toHaveLength(0);
});
